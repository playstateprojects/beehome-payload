# A generated secret for Payload (generate with: openssl rand -hex 32)
PAYLOAD_SECRET=

# Optional: Set to 'production' or 'preview' to target specific environment for sync scripts
# Defaults to 'preview' for dev environment
# CLOUDFLARE_ENV=preview

# Server URL (for production)
# PAYLOAD_PUBLIC_SERVER_URL=https://cms.beehome.net
